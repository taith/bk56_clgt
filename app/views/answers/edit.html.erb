<h1>Editing answer</h1>
<%= if @answer.user_id ==  current_user.id then render 'form'; else  'Permission denied'  end %>

<%= link_to 'Show', question_answer_path(@question, @answer) %> |
<%= link_to 'Back', question_answers_path(@question) %>
